version: '3'

services:
  backend:
    build: ./Backend
    ports:
      - "5000:5000"
    restart: always

  frontend:
    build: ./Frontend
    ports:
      - "80:80"
    restart: always
    depends_on:
      - backend
